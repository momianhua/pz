# 多agent引擎可替换架构实现-任务书

## 背景

Agent网关+Agent引擎（harness）的架构是当前Agent应用的主流架构之一。

例如：
- openclaw：https://github.com/openclaw/openclaw

都采用了类似的架构。

此架构下，Agent网关面向业务演进，负责连接和映射具体业务到底层Agent引擎；Agent引擎面向智能执行能力演进，负责完成实际的任务。举个群助手业务的例子，Agent网关接受群助手请求后，需要映射到底层引擎的一个具体session处理，从而做到同个群的会话连续性、群会话之间上下文的隔离、群助手权限的限制。

业界agent引擎百花齐放，竞争激烈，如pi、opencode、hermes、opendesk、dsh等。Agent应用架构设计时就需要考虑如何在保持Agent应用架构稳定性的同时，快速接入业界先进的harness。

## 描述

参赛者需要基于Agent网关+Agent引擎（harness）的架构，构建一个支持多引擎快速切换的智能体系统。参赛者可以基于任意技术栈，至少完成两种不同的Agent引擎接入。参赛者需要实现赛题定义的agent网关接口（见附件），用于评测。

### 评测数据示例

```json
{
    "task_id": "office_002",
    "title": "打开Outlook客户端",
    "description": "自动打开Outlook邮件客户端",
    "query": "请自动打开 Outlook 邮件客户端",
    "category": "办公协作、沟通与日程管理类",
    "secondary_category": "软件交互",
    "difficulty": 1,
    "difficulty_label": "极易"
}
```

## 规则

### 技术方案要求

本比赛不限制参赛者所使用的技术方案，参赛者在满足基础要求的条件下，可以基于任意技术栈及现有代码实现。

**要求：**
- 代码支持在Windows系统运行，支撑评测
- 可以通过环境变量改变引擎
- 基础网关接口功能必须完成
- 模型使用GLM5.2

**可选技术方案：**
1. 自行实现网关层代码，接入两种以上agent引擎
2. 基于赛题参考的员工助手内源代码，在项目已支持引擎的基础上，至少再支持一个引擎
3. 基于开源代码构建，要求实现题目要求的网关层接口，在项目已支持引擎的基础上，至少再支持一个引擎

**为降低难度，以下要求可选不实现：**
- 可以不实现跨引擎的会话同步
- 可以不实现持久化存储，会话数据可以保存在内存中
- 可以不实现多agent机制，可以每个引擎内置一个默认agent，会话基于该agent创建

### 评价规则

**客观评价指标（占比70%）**

整体采用 Agent 执行 + LLM 裁判打分 的两阶段评测方法：
- 第一阶段 Rollout（执行采集）：让被测 Agent 在受控环境中执行任务，记录完整交互过程
- 第二阶段 Evaluation（裁判打分）：LLM-as-Judge 评估 Agent 执行结果的质量

**主观评价指标（占比30%）**
- 方案、架构合理性（20%）
- 创新性（5%）
- 鲁棒性（5%）

**初赛得分计算：**
对选手引入的引擎都执行一次评测，每个用例取最高分，所有评测用例加起来为该题总分。

### 其它要求

1. 任何作弊嫌疑将被要求提供解释
2. 被证实的作弊将不被允许参与决赛评比
3. 大赛组委会保留追溯的权利，在3个月内被追溯证实的作弊行为成绩将被取消

**注：** 选手需提交源码和编译启动方式，由赛题组在沙箱环境完成评测。

## 作品

### 目录结构

参赛作品打包为 **solution.zip**，解压后的目录结构如下：

```
solution/
├── INSTRUCTION.md        # 裁判执行说明书（编译方式、执行步骤）
├── code/                 # 完整源码（保持项目工程结构，如有sdd产物，也一并提交）
```

各目录说明：
- **INSTRUCTION.md**：裁判执行说明书，用于指导自动评测系统部署、启动、执行参赛作品并获取评测结果。至少包含：
  - **环境准备**：依赖安装、服务启动
  - **执行方式**：启动命令、执行命令、参数说明、调用方式、执行顺序
  - **执行完成判定**：命令退出、服务返回完成状态、指定文件生成
  - **生成结果交付件说明**
- **code/**：完整源码，保持项目工程结构，包含依赖声明和配置文件。

### 重要提示

- 缺少 INSTRUCTION.md 或 INSTRUCTION.md 无法指导自动执行，将导致作品无法完成自动评测
- 执行过程需要人工交互的，需要实现接口供裁判模型自动提交交互，否则将导致作品无法完成自动评测
- 代码必须支持在Windows系统运行
- 必须通过环境变量实现引擎切换
- 基础网关接口功能必须完成
- 至少接入两种不同的Agent引擎
- 受限于判题资源，初赛阶段提交截止后，统一进行一次评测
- 选手需提交源码和编译启动方式，由赛题组在沙箱环境完成评测
