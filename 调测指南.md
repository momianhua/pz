# 多agent引擎可替换架构实现-调测指南

## 环境准备

### 系统要求

- 操作系统：Windows 10/11
- 运行时环境：根据所选技术栈安装对应运行环境（如 Node.js、Python 等）
- 模型服务：需确保GLM5.2模型服务可访问

### 环境变量配置

通过环境变量切换引擎，示例：

```bash
# 切换当前使用的Agent引擎
AGENT_ENGINE=opencode    # 或 pi, hermes 等
```

### 依赖安装

根据所选技术栈安装项目依赖，确保所有依赖包可正常下载。

## 资源说明

### 评测数据集

评测用例为JSON格式，每条用例包含以下字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| task_id | 任务唯一标识 | office_002 |
| title | 任务标题 | 打开Outlook客户端 |
| description | 任务描述 | 自动打开Outlook邮件客户端 |
| query | 用户提问 | 请自动打开 Outlook 邮件客户端 |
| category | 任务分类 | 办公协作、沟通与日程管理类 |
| secondary_category | 二级分类 | 软件交互 |
| difficulty | 难度等级（数字） | 1 |
| difficulty_label | 难度标签 | 极易 |

### 网关接口

参赛者需实现赛题定义的Agent网关接口（详见附件），确保接口功能完整可用。

## 调测步骤

### 第一步：环境验证

1. 确认运行环境安装正确（Node.js/Python 等）
2. 确认GLM5.2模型服务可正常访问
3. 确认环境变量配置生效

### 第二步：单引擎调测

1. 启动网关服务，指定单一引擎（如 `AGENT_ENGINE=opencode`）
2. 发送简单评测用例（如 difficulty=1 的用例），验证基本流程：
   - 网关接收请求 → 映射到引擎 → 引擎执行任务 → 返回结果
3. 检查返回结果是否符合预期
4. 逐步测试不同难度的用例

### 第三步：多引擎切换调测

1. 切换环境变量到另一个引擎（如 `AGENT_ENGINE=pi`）
2. 使用相同用例验证第二个引擎的执行流程
3. 确认两个引擎均可正常工作
4. 反复切换环境变量，验证引擎切换的稳定性

### 第四步：评测流程验证

1. **Rollout 阶段验证**：让Agent在受控环境中执行任务，确认完整交互过程被正确记录
2. **Evaluation 阶段验证**：使用LLM-as-Judge对执行结果进行打分，确认评分流程正常
3. 检查评分结果是否合理

### 第五步：全量用例测试

1. 运行全部评测用例
2. 统计各引擎的得分情况
3. 分析失败用例，定位问题原因

## 调测结果

### 结果分析

- 每个用例取多引擎中的最高分
- 所有评测用例得分之和为该题总分
- 关注客观指标（70%）和主观指标（30%）的综合表现

### 验证方法

- 对比不同引擎在相同用例上的执行结果
- 检查网关接口返回数据的完整性与正确性
- 验证引擎切换后会话隔离是否正常

### 归档要求

- 提交完整源码
- 提交编译启动方式说明
- 由赛题组在沙箱环境完成评测

### 常见问题

待补充
